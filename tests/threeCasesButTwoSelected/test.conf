runnerOptions="-case SecondCase_test -case FirstCase_test"
checkNbCases="2"
checksStr+=( "All test should succeed"="All tests succeed" )
checksStr+=( "Case selection is correct"="Start testing of FirstCase_test" )
checksStr+=( "Case selection is correct"="Start testing of SecondCase_test" )
